{
  "name": "sales-repo-service",
  "version": "1.0.0",
  "description": "Persistent grocery sales repository with uploads, aggregation, and CSV export.",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
  "start": "node --max-old-space-size=512 server.js",
  "dev": "nodemon --watch . --ext js,html,css --signal SIGINT server.js"
  },
  "dependencies": {
    "@fast-csv/format": "^5.0.0",
    "csv-parse": "^5.5.5",
    "date-fns": "^3.6.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "xlsx": "^0.18.5",
    "better-sqlite3": "^9.6.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
